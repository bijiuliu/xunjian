# 夜班巡检项目交接文档

> 更新日期：2026-08-22  
> 用途：让新的 Codex 对话在几分钟内接入项目，避免重新梳理纸表、重复设计或恢复已经删除的内容。

## 1. 60 秒接手

- 项目目录：`E:\codex\我的项目\夜班巡检`
- 核心页面：`src/app/page.tsx`
- 本地地址：<http://localhost:3000/>（`127.0.0.1:3000` 亦可）
- GitHub 仓库：<https://github.com/bijiuliu/xunjian>
- 已部署地址：<https://bijiuliu.github.io/xunjian/>
- 技术栈：Next.js 16.3.2、React 19、TypeScript、Tailwind CSS 4、shadcn/ui 风格本地组件、Lucide Icons、Framer Motion、Sonner
- 产品形态：只适配手机端的 App 风格 PWA，用来替代不方便随身携带的纸质夜班巡检表。
- 数据方式：全部保存在浏览器 `localStorage`；历史记录键为 `night-inspection`，填写中的草稿键为 `night-inspection-draft`；没有服务端和云同步。
- 主导航顺序：`8#冲渣` → `皮带` → `9#冲渣` → `历史记录`；一级导航可吸顶，皮带子导航在其下方吸顶。
- 当前必须保留：深色渐变首页头部、移动端圆角卡片、蓝色主操作、iPhone Safe Area，以及方案 C 的双层导航视觉。
- 设计规范入口：`DESIGN.md`；全局 token 唯一来源：`src/app/globals.css`。导航嵌套圆角必须满足“内圆角 = 外圆角 − 内边距”。
- 当前已知 Bug：快速连续切换一级板块时，导航选中态和下面的内容偶尔不同步。曾尝试取消退出动画等待，问题可消失，但用户认为动画不够细腻并要求撤销，因此当前代码仍保留 `AnimatePresence mode="wait"`。

接手后先读取：

1. `AGENTS.md`
2. 本文档
3. `README.md`
4. `src/app/page.tsx`
5. `next.config.ts` 与 `.github/workflows/deploy.yml`

不要仅依据本文直接写 Next.js 代码。项目的 `AGENTS.md` 明确要求：修改代码前先阅读本地 `node_modules/next/dist/docs/` 中与改动有关的 Next.js 16 文档。

## 2. 产品目标与范围

这是一个夜班现场巡检记录工具。核心目标不是把纸表做成桌面 Excel，而是在手机上快速填写、离线留存，并在保存后生成便于查看和誊写的汇总。

四个板块：

1. 8#冲渣区域
2. 皮带区域，子板块顺序为 SZ101、SZ201、SZ201-N
3. 9#冲渣区域
4. 历史记录：查看保存后的整合汇总、管理和删除记录

纸表底部的日期、班次、巡检人、巡检时间不照搬成输入项；保存时自动生成填写日期和时间。

## 3. 纸表解释与明确排除项

- 纸表里的黑色实心点是“该设备没有这个点位”的占位符，不代表异常，也不是需要填写的状态。
- 纸表中的斜线表示勾选；正常状态默认是勾，点击后可切换为叉。
- 不显示数值单位。
- 数值输入统一调用手机数字键盘，并限制最多两位。
- 未填写值必须保持为空，不要自动填 `0` 或 `1`。之前“全部填 1”只用于临时测试保存效果。

以下纸表底部内容明确不做：

- 8#、9#冲渣液压站油位
- 冲渣料斗下料口
- SZ201、SZ201-N 皮带头轮下料口
- 泵房四段管沟排水
- 8#、9#炉地下管廊排水
- 9#冲渣沟无漏水
- 粒化塔周边阀门无漏水
- 9#冲渣泵东侧集水坑排水
- 消防水池液位
- 车库南跨卫生打扫等“其他事项”

## 4. 8#与9#冲渣业务规则

### 泵号

| 区域 | 设备组 | 可选泵号 |
|---|---|---|
| 8#冲渣 | 冲渣泵 | 101、102、103 |
| 8#冲渣 | 上塔泵 | 501、502、503 |
| 9#冲渣 | 冲渣泵 | 201、202、203 |
| 9#冲渣 | 上塔泵 | 1#、2#、3# |

每组都是三台泵、两用一备，页面只记录两台正在运行的设备。每台运行设备填写四个数值点：

- 南
- 北
- 前轴
- 机身

### 重复泵号选择逻辑

同一设备组的两张运行泵卡片不能最终保持相同泵号。当前实现不是把重复项禁用，而是交换：

- 当前卡片为空，选中另一张卡片已有的泵号：当前卡片得到该泵号，另一张变为空。
- 当前卡片已有泵号，再选中另一张已有泵号：两张卡片的泵号互换。

对应实现为 `src/app/page.tsx` 中的 `choosePump`，不要恢复成“重复编号变灰不可选”的旧方案。

### 当前代码中的状态项

当前每张泵卡片仍包含“盘根引水槽”，默认显示正常勾，点击切换为叉。它曾在需求沟通过程中被提过删除，但当前源码确实存在；后续若要删除，先向用户确认范围，不要凭旧截图自行处理。

## 5. 皮带区域业务规则

### 基本含义

- SZ101、SZ201、SZ201-N 是具体皮带编号。
- SZ101 常规两轴方向为东、西。
- SZ201、SZ201-N 常规两轴方向为南、北。
- “滚面”原本是滚筒表面状态，但所有滚面勾选 UI 已按要求移除。
- 皮带情况、高速/低速联轴器、头轮气管、尾轮下料口等独立勾选卡片已经移除，不要恢复。
- 每个皮带项目卡片右上角有“清空”，只清空该卡片的输入。

### 当前可见项目（以源码为准）

| 皮带 | 当前项目与特殊规则 |
|---|---|
| SZ101 | 电机（单输入）、头轮、头增面轮、尾轮；没有液力耦合器、配重类、中间滚筒 |
| SZ201 | 电机/减速机、头轮、配重东/南、配重西/北、配重、中间滚筒、尾轮；没有液力耦合器、头增面轮 |
| SZ201-N | 电机/减速机、液力耦合器（单输入，输入标签“液耦”）、头轮、头增面轮、配重东/南、配重西/北、配重、尾轮；没有中间滚筒 |

方向标签规则：

- 普通双点项目：使用皮带本身的两端方向。
- 电机/减速机：两个输入标签为“电机”“减速机”。
- 配重东/南：两个输入标签为“东”“南”。
- 配重西/北：两个输入标签为“西”“北”。
- 配重东/南、配重西/北标题前有实心圆点；只有“配重”二字的标题前没有圆点。

注意：需求经历过多轮撤销和反向修改，皮带项目必须以 `visibleBeltItems`、`beltItemTitle`、`beltPoints` 的当前代码为最终依据。

## 6. 已完成功能

- 四个一级板块和三个皮带子板块。
- 冲渣泵号下拉选择、重复泵号交换逻辑。
- 所有数值输入统一为数字键盘、最多两位、黑色数字。
- 冲渣卡片与皮带项目卡片的局部清空。
- 正常勾/异常叉状态切换。
- 保存前完整性检查：分别列出未选择的泵号和空白数值项，并允许“返回补充”或“仍然保存”。
- 本地保存、自动填写日期和时间。
- 填写中的草稿自动保存：输入值、泵号选择、勾/叉状态和当前皮带子板块会在重开页面后恢复；只有点击“新建”才清空草稿并将皮带子板块重置为 `SZ101`。
- 点击冲渣泵或皮带项目的“清空”后，在页面顶部中央显示 Sonner 成功提示，并带上对应的区域或皮带编号。
- 历史记录列表、详情汇总、管理、多选批量删除、详情页删除和二次确认。
- 汇总顺序：皮带区域 → 8#冲渣 → 9#冲渣。
- GitHub Pages 静态部署。
- PWA manifest 与 iPhone Safe Area 适配。
- 双层导航吸顶：一级导航为 44px 高的白色工具栏，选中项为蓝色实心；皮带子导航为 42px 高的浅灰工具栏，选中项为白底蓝字。子导航吸顶时位于一级导航下方并保留约 8px 间距。
- 双层导航的外层圆角为 22px、内边距为 4px、选中项圆角为 18px，四边保持等距和同心视觉关系。
- 卡片“清空”操作保留至少 44px 的实际触控区域；视觉文字恢复为原有小号常规字重，不能为了扩大触控范围同步放大文字。
- 历史记录列表与详情已增加双向入口/返回动画；详情底部“返回历史记录 / 删除”操作栏已移出水平变换容器，固定相对视口和 Safe Area 定位，不再从 9# 卡片下方生成后跳到底部。

## 7. UI 约束

- 只做手机端，不需要桌面布局。
- 采用现代 iOS App 感：浅灰页面、白色圆角卡片、蓝色主操作、适度阴影。
- 首页头部保持深色渐变，当前配色是 `from-slate-950 via-slate-900 to-blue-950`。
- 一级导航采用方案 C：白色圆角工具栏、蓝色实心选中态、白字；皮带子导航保持浅灰底、白色选中态、蓝字。导航分别为 44px 与 42px，不要因视觉层级缩小手机触控区域。
- 导航轨道使用 4px 内边距：外层 22px 圆角，选中项 18px 圆角。不得只调整其中一个半径，否则内外曲线会失去平行关系。
- 冲渣泵选择器位于设备名称后、清空按钮前；宽 108px、高 44px，默认文案为“选择”。仍使用原生 HTML `<select>` 以保留系统选择菜单；蓝色焦点圈的 `.18s` 过渡动画已按要求保留。
- “清空”按钮的可点击区域与视觉文字必须分开控制：触控区域至少 44px，文字沿用 `text-caption` 和原有字重，不改成大号粗体。
- 清空成功提示位于页面顶部中央；底部区域用于历史记录删除等操作，不要将清空提示移到底部。
- 不要重新引入 Ionic；项目已经从 Ionic 方案重做。
- 不要恢复已经删除的文案和装饰，例如“巡检记录”“夜班工具”“离线可用”。
- 用户经常要求“只修改框选部分”，不要顺手调整周边字体、字号、颜色或间距。
- 历史汇总排版经过多轮对齐调整。除非明确要求，只改指定布局，不改字体、字号和卡片样式。
- 删除入口应维持当前实现，不要恢复已经撤销的左滑删除。

### 当前动画参数

动画应短、自然、克制，不得阻碍现场连续操作。当前源码参数如下：

| 位置 | 单段时长 | 用户感知 | 备注 |
|---|---:|---:|---|
| 一级板块内容切换 | 140ms | 约 280ms | `mode="wait"`，退出与进入顺序执行 |
| 历史列表进入详情 | 140ms | 约 280ms | 退出 140ms 后进入 140ms，轻量水平推进 |
| 历史详情返回列表 | 140ms | 约 280ms | 方向与进入动画相反 |
| 历史详情底部操作栏 | 140ms | 140ms | 入场对齐详情内容入场，返回时与详情内容同步退场 |
| 一级导航选中态 | 200ms | 200ms | Tailwind CSS transition |
| 皮带子导航选中态 | 200ms | 200ms | Tailwind CSS transition |
| 通用 Button | 200ms | 200ms | 包括按压缩放至 0.97 |
| 输入框焦点状态 | 150ms | 150ms | Tailwind 默认 transition |
| 原生 Select 焦点状态 | 180ms | 180ms | 边框、阴影与缩放 |
| Dialog 遮罩 | 300ms | 300ms | Framer Motion 默认 tween |
| Dialog 面板 | 无固定时长 | 动态结束 | Spring：stiffness 420、damping 34 |

Sonner 的主要内部动画为：Toast 位移/透明度 400ms、图标淡入 300ms、阴影 200ms、关闭按钮透明度 100ms、滑动移除 200ms、折叠移除位移 500ms、Spinner 单圈 1200ms。

系统启用“减少动态效果”时，CSS 动画被压缩至 `0.01ms`，历史列表/详情和底部操作栏降为 0ms，Sonner 关闭动画。一级板块切换和两个 Dialog 的 Framer Motion 动画尚未完整接入该设置，属于待统一项。

## 8. 数据与离线能力

数据结构位于 `src/app/page.tsx`：

```ts
type InspectionRecord = {
  id: string;
  date: string;
  time: string;
  values: Record<string, string>;
};
```

- 存储：浏览器 `localStorage`。
- 历史记录键：`night-inspection`，结构为 `InspectionRecord[]`。
- 草稿键：`night-inspection-draft`，结构为 `{ values, beltTab }`；代码兼容旧版仅保存 `values` 的草稿。草稿会随填写自动更新，点击“新建”清空。
- 无账号、数据库、接口或云同步。
- 清理浏览器站点数据、换浏览器或换手机会丢失历史记录。
- 当前只有 manifest，不等于完整离线缓存；没有 Service Worker 对首次加载后的所有静态资源做可靠缓存。若要宣称“完全离线”，需要另行实现并测试。

## 9. 架构与部署

关键文件：

- `src/app/page.tsx`：主要业务、状态、页面和历史记录逻辑，目前集中在一个客户端组件中。
- `src/app/globals.css`：全局样式与 Safe Area。
- `src/components/ui/button.tsx`、`card.tsx`：本地 shadcn/ui 风格组件。
- `next.config.ts`：静态导出与 GitHub Pages 子路径。
- `.github/workflows/deploy.yml`：GitHub Pages 构建部署。
- `public/manifest.webmanifest`：PWA manifest。

`next.config.ts` 当前关键配置：

```ts
output: "export"
basePath: process.env.PAGES_BASE_PATH
allowedDevOrigins: ["127.0.0.1", "localhost"]
```

GitHub Actions 使用 Node 22 构建并部署。交接文档历史记录显示曾推送提交 `c47e753`（恢复选择器焦点过渡动画），GitHub Pages 地址为 <https://bijiuliu.github.io/xunjian/>。当前本机目录无法通过 Git 核验远端最新提交，不要将该提交号视为当前线上版本证明。

本机目录不一定已经配置成可直接 `git push` 的完整 Git 工作区，且本机曾没有可用的 GitHub CLI 登录。新对话必须先运行只读的 `git status`、`git remote -v` 检查，不要默认推送链路已经配置。

## 10. 当前已知问题与后续候选

### 已知问题

1. 快速连续点击一级导航时，选中标签与内容偶尔错位。根因方向集中在带退出等待的 keyed `AnimatePresence mode="wait"`。修复时要同时保持动画细腻，不能简单删除全部动画。
2. ESLint 不是完全干净：历史上存在未使用的 `checks`、effect 内同步 setState 读取 localStorage 等问题。TypeScript 和生产构建可通过，但不要声称 lint 已全绿。
3. `src/app/page.tsx` 体积较大，UI、业务映射、存储和历史记录耦合在一起；重构要先补行为验证，避免改变现有数据键。
4. 只有 localStorage，没有数据导出、备份和跨设备同步。
5. 只有 PWA manifest，没有完整 Service Worker 离线缓存。
6. “减少动态效果”目前只覆盖 CSS、Sonner、历史详情切换和历史底部操作栏；一级板块切换及保存/删除 Dialog 仍需统一。

### 尚未实现

- APK。可用 Capacitor 包装当前 Next.js 静态导出，不需要 Ionic UI；目前未安装或配置 Capacitor。
- 云端同步、登录、多设备历史。
- 历史数据导出/导入和备份。

## 11. 开发与验证

先确认依赖已经安装，再执行：

```powershell
npm run dev
```

本地访问：<http://localhost:3000/>（也可使用 <http://127.0.0.1:3000/>）

基础验证：

```powershell
npx tsc --noEmit
$env:PAGES_BASE_PATH='/xunjian'
npm run build
```

每次涉及交互时至少手工检查：

- 8#、皮带、9#、历史记录能正常切换。
- 快速连续切换不会出现标签与内容错位。
- 两台运行泵选择重复编号时按交换规则工作。
- 所有输入只能保留最多两位数字，并能在手机唤起数字键盘。
- 卡片清空只影响当前卡片。
- 清空后顶部成功提示的文案与区域/皮带编号匹配。
- 填写输入、泵号选择和皮带子板块在刷新页面后仍能恢复；点击“新建”后草稿清空且子板块回到 `SZ101`。
- 滚动时一级导航固定在顶部安全区下方；在皮带区域时，子导航固定在一级导航下方，二者不重叠。
- 保存校验能准确区分未选泵号与空输入。
- 保存后汇总顺序、日期时间、删除功能和 localStorage 持久化正常。
- GitHub Pages 构建时 `/xunjian` 子路径资源正常。
- 历史列表进入详情和返回列表时方向正确；底部操作栏从开始到结束都固定在视口底部，不得先出现在内容流内再跳动。
- 导航轨道四边内边距均为 4px，外层 22px 与内层 18px 圆角视觉平行。
- 卡片“清空”文字保持原有视觉字号，同时完整触控区域不小于 44px。

## 12. 新对话的工作原则

1. 先查当前代码和本文，不重新访谈已经明确的纸表需求。
2. 不因旧截图与当前源码不同就直接回滚；需求有大量“撤销”，当前代码优先。
3. 一次只修改用户指定范围，保留未被要求改变的 UI。
4. 修改数据 key 或历史结构前，必须考虑已有 localStorage 记录兼容。
5. 修改部署前先确认静态导出和 GitHub Pages `basePath`。
6. 新增依赖前先检查现有 `package.json` 和上级共享依赖，不重复下载。
7. 完成后给出实际验证结果；不要只说“应该可以”。

## 13. 新建对话时直接粘贴

```text
继续开发 E:\codex\我的项目\夜班巡检。

先完整阅读：
1. AGENTS.md
2. PROJECT_HANDOFF.md
3. README.md
4. src/app/page.tsx

不要重新设计、不要重复实现已完成内容。先根据交接文档说明当前状态，再执行我的新需求。
```
